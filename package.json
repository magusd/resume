{
  "scripts": {
    "dev": "resume serve --theme stackoverflow",
    "build": "resume export resume.pdf --format pdf --theme stackoverflow",
    "build-html": "resume export build/index.html --format html --theme stackoverflow"
  },
  "dependencies": {
    "jsonresume-theme-stackoverflow": "^2.0.2",
    "resume-cli": "^3.1.2",
    "resumed": "^3.0.1"
  }
}
